<Application x:Class="Приложение.Windows.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Приложение"
             StartupUri="/Windows/MainWindow.xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
      
            <Style x:Key = "UpperPanelMainWindowButton" TargetType = "Button">
             
                <Setter Property = "Foreground" Value = "White" />
                <Setter Property = "FontSize" Value = "12" />
                <Setter Property = "Margin" Value = "10" />
            <Setter Property = "Height" Value = "70" />
            <Setter Property = "Width" Value = "350" />
            <Setter Property = "Template">

                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">

                        <Border x:Name="Border" CornerRadius="0,0,4,4" BorderBrush="Gray" BorderThickness="1,1,1,3" Background="white"
                            TextBlock.Foreground="Black"
                             TextBlock.FontSize="30"
                             TextBlock.FontWeight="Medium"
                             TextBlock.FontFamily="Corbel Light">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key = "CreateTestWindowButton" TargetType = "Button">
            <Setter Property = "FontSize" Value = "2" />
            <Setter Property = "Template">
                
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">

                    <Border x:Name="Border" CornerRadius="2" BorderBrush="Gray" BorderThickness="1,1,1,1" Background="white"
                            TextBlock.Foreground="Black"
                             TextBlock.FontSize="12"
                             TextBlock.FontWeight="Medium"
                             TextBlock.FontFamily="Corbel Light">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
            </Setter>
        </Style>


    </Application.Resources>
</Application>
